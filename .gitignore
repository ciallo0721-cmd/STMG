# 虚拟环境 / 缓存
.venv/
__pycache__/
*.py[cod]

# 构建产物
dist/
build/
*.spec

# 运行期数据
.stmg_save/
.stmg_launcher.json

# 自己的项目不进仓库（只留一个占位文件保住空目录）
projects/*
!projects/.gitkeep

# 编辑器
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini

# 项目记忆 / 工作区数据（不进仓库）
.workbuddy/

# Pyodide 运行时（tools/get_pyodide.py 本地下载，约 13MB 不进仓库）
stmg/pyodide/

# 临时测试
_tmptest/
_devtest/
*.bak
