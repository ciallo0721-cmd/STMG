# 📋 STMG 功能建议报告（第 2 期 · 增量版）

> 基于真实源码重新盘点，**已剔除第 1 期（2026-09-13）中已经落地的功能**。
> 生成时间：2026-09-17 19:53

## 项目概况

- **类型**：桌面视觉小说引擎 + 工具链（Python 3.8+ / pygame / CustomTkinter / Pyodide）
- **代码规模**：引擎 9 个模块约 4,500 行，launcher.py 约 10 万字节，工具链 8 个脚本约 3,000 行
- **现有功能数**：约 45 项核心能力（含第 1 期落地项）
- **已有输出形态**：桌面 exe / 加密 `.stmdec` / 网页 HTML（wasm 或 js 引擎）/ Ren'Py `.rpy` / 可视化编辑器 / 美化包

## ✅ 第 1 期建议已全部落地（本期不再重复）

| 第 1 期建议 | 落地证据 |
|---|---|
| 跳过已读 | `uiconf.py:96 skip_read` |
| 打字机逐字音效 | `uiconf.py:97 type_se` |
| 立绘逐句 scale/alpha/y/expr | `runtime.py:528-556`、`session.py:103-111` |
| 立绘旋转 / 空闲微动 | `render.py:268 draw_sprite`、`uiconf.py:57-59` |
| 转场特效（fade/dissolve/flash） | `runtime.py:341`、`render.py:413` |
| 随机内建 `RAND()` + 回放感知 | `runtime.py:58,573` |
| 成就 / 收集系统 | `runtime.py:600`、`gui.py:1058`、`save.py:79-120` |
| CG 回廊 | `gui.py:580-622`、`save.py:124-160` |
| 多语言 i18n | `project.py:393 find_languages`、`gui.py:547` |
| 可视化剧本编辑器 | `launcher.py:672,714-724`（line/blocks/code 三模式 + 7 类积木箱） |
| 错误提示友好化 | `errors.py:89 report`、`gui.py:972-992` |
| 加解密解包器 | `stmenc/unpack.py` |
| 美化包（mod）扩展系统 | `mod.py`（742 行）+ `tools/modcheck.py` + `tools/modpub.py` |

> 换句话说：**第 1 期的路线图已经清空了**，本期是「清空之后」的下一层缺口。

## 现有功能清单（成熟度）

| 功能 | 成熟度 | 说明 |
|---|---|---|
| 剧本语法（say/choose/if/else/question/set/define/sprite/label/jump/pycode/stm.os） | polished | `parser.py:313-503` |
| 事件流（17 种事件） | polished | `runtime.py:7-21` |
| 多立绘 + tag/pos/scale/alpha/y/rotate/expr | polished | 无补间动画 |
| 三通道音频 | functional | 无淡入淡出、不支持 flac |
| 转场 / 跳过已读 / 打字机音效 | polished | 第 1 期落地 |
| 成就 + CG 回廊 + i18n | functional | 跨会话持久化 |
| 存档（重放式，6 格） | basic | `session.py:159-166` 只存 answers + say_count |
| 网页版（真引擎 wasm / 内置 js） | functional | 1 自动 + 6 手动存档，localStorage |
| 可视化编辑器（三模式 + 积木） | functional | 无预览、无素材面板、单文件 |
| 美化包系统 | polished | 三条硬性限制 + 静态校验 |
| 打包 / 发布 HTML / Ren'Py 双向转换 | polished | 见 `tools/` |
| 自检 selftest（94 项断言 / 11 个函数） | polished | 无 CI 承载 |
| 启动器（CustomTkinter） | polished | 项目管理 + 全工具入口 |

## 功能建议（按投入分组，组内按优先级分降序）

优先级分 = Impact×3 + (4−Effort)×2；Effort：低=1/中=2/高=3。

### 🟢 快速见效（低投入，优先做）

| 功能 | 分类 | 投入 | 回报 | 分 |
|---|---|---|---|---|
| 设置面板扩充（字号 / 行距 / 字体 / 关打字机 / 文本框透明度） | 用户体验 | 低 | 高 | 15 |
| 快速存读 + 存档缩略图（F5/F9 直接对当前槽操作） | 用户体验 | 低 | 高 | 15 |
| 语音重播按钮 + 角色语音自动挂载约定 | 功能扩展 | 低 | 高 | 15 |
| README「已知限制」章节同步（已严重过期） | 开发者体验 | 低 | 中 | 12 |
| GitHub Actions CI（push 自动跑 selftest + check） | 开发者体验 | 低 | 中 | 12 |
| 剧本健康度报告 `tools/doctor.py`（分支/孤儿 label/未用素材） | 开发者体验 | 低 | 中 | 12 |

### 🟡 值得投入（中投入，规划做）

| 功能 | 分类 | 投入 | 回报 | 分 |
|---|---|---|---|---|
| 状态快照存档（替代重放式，彻底修读档失配） | 功能扩展 | 中 | 高 | 13 |
| 立绘补间动画（移动 / 淡入淡出 / 排队演出） | 功能扩展 | 中 | 高 | 13 |
| 编辑器实时预览（改剧本即热重载小窗） | 开发者体验 | 中 | 高 | 13 |
| 编辑器素材面板（素材树 / 拖入插入 / 缺失高亮） | 开发者体验 | 中 | 高 | 13 |
| 镜头演出（震屏 / 推拉 / 平移） | 功能扩展 | 中 | 中 | 10 |
| 音频增强（淡入淡出 / flac·ogg / BGM 列表与单次播放） | 功能扩展 | 中 | 中 | 10 |
| 存档导入导出（跨设备迁移、分享给朋友） | 用户体验 | 中 | 中 | 10 |
| 素材工作流（批量导入 / 自动缩放压缩 / 命名规范检查） | 开发者体验 | 中 | 中 | 10 |
| 美化包索引 + 一键安装（mod 站雏形） | 集成/生态 | 中 | 中 | 10 |

### 🔵 长远目标（高投入，列入路线图）

| 功能 | 分类 | 投入 | 回报 | 分 |
|---|---|---|---|---|
| 移动端打包（Android 优先，复用无头 Runtime） | 集成/生态 | 高 | 高 | 11 |
| 多文件剧本（include / 跨文件 label） | 功能扩展 | 高 | 中 | 8 |
| call / return 子程序 + 循环（repeat / while） | 功能扩展 | 高 | 中 | 8 |
| 网页版 `R.api` 异步 + `stm.os` 沙箱虚拟文件系统 | 集成/生态 | 高 | 中 | 8 |
| 增量更新（仅更新资源包 / 差分） | 性能优化 | 高 | 中 | 8 |

---

## 每个建议的详细说明

### 1. 设置面板扩充
- **描述**：现在游戏内设置只有文字速度、自动等待、三路音量、全屏（`gui.py:29-34`）。中文 galgame 玩家最常调的「字号 / 行距 / 字体 / 关打字机 / 文本框透明度」一个都没有——`uiconf.py` 里早就支持这些字段，只是没接到 UI 上。
- **实现方案**：`gui.py` 的 `draw_menu`（`:851-866`）加一栏「显示」，写回 `Settings` 并落盘；渲染侧让 `uiconf` 的 `text_size / box_alpha / text_speed` 运行时可覆盖（`render.layout` 已按 `base_size` 参数走，只需把设置值传进去）。
- **风险/前置**：纯增量；注意 `Settings.save`（`:48-55`）字段要加进 JSON，旧 cfg 缺字段走默认值即可。

### 2. 快速存读 + 存档缩略图
- **描述**：F5/F9 目前只是「打开存读档对话框」（`gui.py:435-467`），不是快速存/读；存档界面也没有画面缩略图，玩家只能靠一句台词认档。
- **实现方案**：加 `Shift+F5 / Shift+F9` 直接写/读槽 1（或最近使用槽）；存档时把当前 `pygame` 画面 `pygame.transform.smoothscale` 成 160×90 存进 `.stmg_save/slot_n.png`，`draw_saveload`（`:921-948`）读出来当缩略图。
- **风险/前置**：`save.py` 需多写一个附属文件；网页版可同步用 canvas `toDataURL` 存 localStorage（体积要缩到 JPEG 小尺寸）。

### 3. 语音重播按钮 + 角色语音自动挂载
- **描述**：`S.voice()` 只是「播一个文件」（`audio.py:89-96`），没有 galgame 标配的「重听上一句语音」；也没有按角色自动找语音的约定，导致逐句配音要手写每一句路径。
- **实现方案**：① 记录最后一句语音路径，在菜单/文本框角上加「重听」按钮调 `play_voice`；② 约定 `voice/<角色tag>/<台词序号 4 位>.wav`，`S.character(..., voice=True)` 或 `options.stm` 开 `AutoVoice=true` 时由 runtime 在 `say` 事件上自动挂 voice 路径，找不到就静默跳过。参照 Ren'Py 的 `voice` 目录习惯。
- **风险/前置**：`options.py` 加字段；自动挂载失败必须静默（缺配音是常态）；存档重放时要避免重播整段语音（`runtime.quiet` 已有关闭机制，复用它）。

### 4. README「已知限制」章节同步
- **描述**：`README.md:940-954` 仍写着「没有转场特效 / 立绘不支持缩放 / 没有 CG 回廊、多语言、跳过已读 / 错误提示偏开发者向」——这些全都做完并提交了。对刚 clone 下来的人（和未来的你自己）是明确误导。
- **实现方案**：重写「已知限制」，只留真限制：flac 不支持、网页版无 `R.api`/`stm.os`、`file://` 直开不跑 wasm、Ren'Py 单向尽力转换、宏观「无移动端」。顺带把「特性」清单补上成就/CG 回廊/i18n/可视化编辑器/美化包。
- **风险/前置**：纯文档。

### 5. GitHub Actions CI
- **描述**：`tools/selftest.py` 有 94 项断言、`tools/check.py` 能查语法和素材（`selftest.py:39-486`），但**全靠本机手动跑**，项目里没有 `.github/`。引擎改坏了只有你自己知道。
- **实现方案**：`.github/workflows/ci.yml`：`ubuntu-latest` + `python 3.10/3.12` 矩阵，`pip install -r requirements.txt`（pygame 装 `pygame-ce` 或走 `xvfb` + `SDL_VIDEODRIVER=dummy`），跑 `python tools/selftest.py` 与 `python tools/check.py demo`。launcher 相关的 CTk 测试用 `continue-on-error` 隔离。
- **风险/前置**：无头 pygame 需要 `SDL_VIDEODRIVER=dummy`，selftest 里已有「界面跑一帧」的用例，正好验证。

### 6. 剧本健康度报告 `tools/doctor.py`
- **描述**：写长剧本时最容易出的事：孤儿 `label` 没人跳、素材躺在目录里从没被引用、某条线埋了 3 层 `If` 从没被测到。`check.py` 只查语法和「缺失素材」，不查「多余素材」和结构。
- **实现方案**：复用 `parser.parse_file` 的语句树 + `project.info`，输出：台词/分支/选择支统计、平均句长、孤儿 label、未被任何 `S.cg/S.character/S.play` 引用的素材、`If` 嵌套深度 Top 10、每个 `Choose` 的分支数。启动器「开发组」加一个按钮，结果写 `剧本报告.md`。
- **风险/前置**：素材引用可能是变量拼接（`"bg/" + STM.ANS + ".png"`），这类要标为「可能未引用」而不是断言。

### 7. 状态快照存档（替代重放式）
- **描述**：`session.snapshot()`（`session.py:159-166`）只存 `answers + say_count`，读档靠「重放」。`RAND()` 已经做了回放感知（`runtime.py:573`），但 `R.api` 的返回、`stm.os` 的读取结果、以及任何运行时状态都不在快照里——长剧本读档会重跑中途所有副作用。这是当前引擎最大的正确性债务。
- **实现方案**：`snapshot` 增加 `vars`（`runtime.vars` 全量，变量都是 JSON 友好类型）、`chosen`、`history_tail`、以及一个「事件序号指针」；`restore` 优先走「快照直还原」路径（新 `Runtime.restore_from(snap)` 直接灌变量 + 用指针定位到语句树节点），旧档（无 `vars` 字段）回退现有 replay。`save.py` 加 `format: 2` 标记。
- **风险/前置**：需要「语句树节点 ↔ 存档指针」的稳定寻址（用「路径式下标」而非对象引用）；`stm.os` 副作用无法回滚，文档要写清「恢复的是游戏状态，不是玩家电脑状态」。

### 8. 立绘补间动画
- **描述**：现在改立绘位置/大小是瞬切；`draw_sprite` 支持逐句 `scale/alpha/y/rotate`（`render.py:268`），但**没有过渡**，演出很生硬。galgame 里「角色走进画面」「说着说着脸红放大」都是靠补间。
- **实现方案**：`session.scene["sprites"][tag]` 加 `from` / `to` / `t0` / `dur` 三元组，`gui.update(dt)` 每帧插值（线性或 ease-out）；语法上用现有关键字加 `dur=`：`S.character("png/菲.png", pos="left", dur=0.4)`，缺省 `dur=0` 保持瞬切（向后兼容）。同一套插值器可以给 `bg`/`picture` 的 alpha 复用。
- **风险/前置**：存档快照要存「演出终态」而不是插值中间态（插值属展示态）；跳过已读/快进时直接落到终态。

### 9. 编辑器实时预览
- **描述**：可视化编辑器已经能做行级/积木/半代码三模式编辑（`launcher.py:672,714-724`），但改完必须「存盘 → 关编辑器 → 点启动游戏」。改一句台词看一次效果的循环太长，这是编辑器最大的体验断点。
- **实现方案**：编辑器里开一个「预览」小窗（`pygame` 子进程 + 现有 `start.py`，或内嵌 `tkinter` 里的 canvas 走网页版 js 引擎）；做最朴素的方案——点「预览」把当前缓冲区写到 `_preview.stm` 并拉起引擎窗口，关闭即回到编辑器；进阶再做「保存文件 → 引擎 sidecar 热重载」。
- **风险/前置**：pygame 窗口和 CTk 窗口抢焦点的坑；进程生命周期要管好（编辑器崩溃不能留孤儿进程）。已有 `_tmptest/` 目录可以先放临时剧本。

### 10. 编辑器素材面板
- **描述**：现在插背景/立绘全靠手打字路径，打错了要到运行时才看到占位块。素材树是把「写剧本」变成「搭场景」的关键一步。
- **实现方案**：编辑器左侧加一个 `ttk.Treeview`/`CTkScrollableFrame` 列 `bg/ png/ music/ gui/` 目录树（`project.ASSET_DIRS` 已定义），双击素材 → 按类型在光标处插入 `S.cg("...")` / `S.character("...")` / `S.play("...")`；引用计数为 0 的素材灰显。
- **风险/前置**：素材多时列表性能（懒加载子目录）；复制不存在的路径要即时提示。

### 11. 镜头演出（震屏 / 推拉 / 平移）
- **描述**：转场有了，但「画面动起来」的手段只有立绘微动。剧情高潮的震屏、`S.camera(zoom=1.2, dur=0.5)` 推近、横向平移看全景，都是成本不高但演出收益明显的语法。
- **实现方案**：runtime 产出 `camera` 事件（`shake/zoom/pan`），`gui.draw_scene`（`:707`）在绘制前对整个场景 surface 做偏移/缩放（先渲染到 offscreen surface 再 `blit` 变换），复用第 8 条的插值器。
- **风险/前置**：offscreen 渲染有性能开销，需要「无相机效果时走原路径」的分支；缩放后要处理黑边填充。

### 12. 音频增强
- **描述**：`audio.py` 是三通道但很朴素：切换 BGM 硬切（`music.play`）、不支持 flac、没有播放列表。情绪转折全靠音乐切换，硬切会毁掉氛围。
- **实现方案**：① 音量淡入淡出——`gui.update` 里对 `pygame.mixer.music.set_volume` 做时间插值，`S.play(..., fade=1.0)`；② flac 走 `pygame.mixer.Sound`（SDL_mixer 视版本支持）或打包时自动转 ogg（需 ffmpeg 可选依赖，缺了就提示）；③ `S.play("a.mp3,b.mp3")` 顺序播放列表 / `loop=False` 播完静音。
- **风险/前置**：ffmpeg 属于可选外部依赖，不能进 `requirements.txt` 硬依赖；淡入淡出与 `stop` 事件的交互要理清。

### 13. 存档导入导出
- **描述**：存档现在只在本机 `.stmg_save/`，换电脑、给朋友看结局、备份二周目都做不到。
- **实现方案**：存读档界面加「导出存档 / 导入存档」（`filedialog` + 把 slot JSON 打成单个 `.stmgsav`），导入时校验 `script` 路径与标题。网页版对应「导出为文件 / 从文件导入」，与桌面版格式互通。
- **风险/前置**：导入他人存档要注意剧本版本不同导致 `answers` 对不上——正好由第 7 条的快照机制缓解。

### 14. 素材工作流
- **描述**：`check.py:71-88` 只会在终端打印前 12 条缺失素材。素材进项目全靠手动拖进 `bg/png/music`，图片大小/格式也不管——一张 4000px 的背景图会让加载和网页版体积直接失控。
- **实现方案**：① 启动器加「导入素材」批量复制 + 自动按类型归目录（`project.ASSET_DIRS`）；② 可选依赖 Pillow：超尺寸图自动缩到基准分辨率 1.5 倍内、透明 PNG 转 WebP，输出「素材优化报告.md」；③ `check.py` 结果导出成文件而不是只打印。
- **风险/前置**：Pillow 属可选依赖（缺了就跳过优化并提示）；自动转换不能覆盖原文件，必须新目录产出。

### 15. 美化包索引 + 一键安装
- **描述**：美化包体系（`mod.py` 742 行 + 三重硬性限制 + 校验）已经相当完整，`modpub.py` 能产出 `.zip`——但装包还是「自己解压到 mod/」。缺一个「发现 → 安装」的环节。
- **实现方案**：① 约定一个远端 `mods.json` 索引（GitHub Pages 静态文件即可，无需后端）：`{id, name, author, version, zip_url, sha256, preview.png}`；② 启动器「美化包」对话框加「在线索引」页，拉索引 → 校验 sha256 → 解压到 `mod/`；③ 装完复用现有 `mod.verify` 做静态校验，不通过则拒绝启用。
- **风险/前置**：⚠️ 这是唯一会「下载并执行别人提供的文件」的功能，必须强制走 `verify`（现有三条限制正好是安全网）；索引地址与 HTTPS 要写死，避免任意源。

### 16. 移动端打包
- **描述**：`Pack=` 字段至今注着「留给以后上手机」（`README.md:235`）。引擎架构其实很友好——`Runtime` + `Session` 完全不依赖 pygame（`session.py` 头注释明说可无头跑），换掉渲染层就能上移动端。
- **实现方案**：Android 优先，两条路：① 保留 pygame 逻辑、用 `pygame-ce` 的 Android 移植（pygame 官方路子，成本低但生态弱）；② 只复用 `parser/runtime/session`，UI 换 Kivy 或直接复用**网页版**那套 `webplayer.html`（用 WebView 套壳 + wasm 引擎）——后者几乎零渲染层开发。
- **风险/前置**：高投入；`stm.os` 在手机上语义完全不同（沙箱化的 App 目录）；触屏要重做交互（点击推进够用，存档/回廊要触屏化）。

### 17. 多文件剧本
- **描述**：现在整个游戏就一个 `script.stm`（教程项目已经 141 行）。真做长篇，多人协作、分章节、复用公共演出（如「每周目都一样的开场」）都需要拆分。
- **实现方案**：`include` 语句 + 跨文件 `label`：`include "chapter1.stm"` 在解析阶段展开（保留行号映射，错误报「chapter1.stm:12」）；或更 Ren'Py 一点：同目录下自动加载 `*.stm`，`S.jump("label")` 全局可见。
- **风险/前置**：行号/say_count 的全局唯一性要重新设计（影响存档）；`find_languages` 的 `script.<lang>.stm` 命名约定要与多文件共存。

### 18. call / return + 循环
- **描述**：`S.jump` 是单程跳转，没有返回栈（`runtime.py:290 do_call` 处理的是调用语句而非子程序）；也没有 `repeat/while`。公共段落（「确认一下状态」「每次进宿舍都一样的描写」）现在只能复制粘贴。
- **实现方案**：`Call "标签"` / `Return` 用调用栈实现（`S.jump` 增加 `push` 语义）；`Repeat 3:` / `While 条件:` 复用 `If` 的缩进子块解析，加迭代上限保护（防死循环卡死发布版）。
- **风险/前置**：与存档重放强耦合（调用栈要进快照，第 7 条是前置）；无限循环必须有硬上限 + 友好报错。

### 19. 网页版 `R.api` 异步 + `stm.os` 沙箱
- **描述**：`web_bridge.py:27-32` 直接挡掉 `R.api`，`stm.os` 静默跳过——同一份剧本桌面版能跑、网页版残缺。
- **实现方案**：`R.api` 改异步：Python 侧发事件给 JS `fetch`，用 Pyodide 的 `asyncio`/回调把结果写回 `STM.API` 再继续推进（`web_bridge.Driver.next` 已是「一问一答」结构，加一个 `pending_net` 状态即可）。`stm.os` 映射到 IndexedDB 虚拟目录，UI 明确标注「浏览器沙箱」。
- **风险/前置**：`R.api` 的 CORS 与密钥暴露问题在浏览器里无法回避（key 会明晃晃在 JS 里），要在发布时警示；跨域 API 大多需要服务端中转。

### 20. 增量更新
- **描述**：`pack.build` 是全量拼接素材（`pack.py:43-74`），改一张图就要重新分发整个 `assets.stmdec`。发过游戏的人都知道这很痛。
- **实现方案**：`assets.stmdec` 索引里存每个资源的内容哈希，`tools/` 加 `patch.py`：对比两个版本的索引 → 只导出被改动的资源 + 新索引，产出「补丁包」；游戏启动时检测补丁包并合并。
- **风险/前置**：索引与 HMAC 机制要一起改（现在整包一个 HMAC）；补丁包的签名/来源校验是必须的。

---

## 总结建议

引擎的「玩家可见功能」这一层其实已经相当完整了——第 1 期清单全部落地，转场、成就、回廊、i18n、可视化编辑器、美化包体系一个不缺。**当前最值钱的不是再加功能，而是补三块地基**：

1. **存档正确性**（第 7 条）：重放式存档是唯一的系统性隐患，越晚改越贵。
2. **创作者工作流**（第 9、10、6 条）：编辑器有预览和素材面板之后，写剧本的循环才会真正变快。
3. **工程可信度**（第 5、4 条）：CI 一挂上，之后每次改引擎都不用自己手动跑 94 项断言。

如果只动一件：**先做第 2 条（快速存读 + 缩略图）**——半天工作量，玩家感受立刻不一样；紧接着做第 7 条（状态快照存档）把地基补牢。

> 想动手的话，告诉我编号，我从改动最小、风险最低的那条开始。
