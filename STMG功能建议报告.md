# 📋 STMG 功能建议报告

> 基于真实源码盘点（parser.py / runtime.py / session.py / uiconf.py / render.py / gui.py）
> 生成时间：2026-09-13

## 项目概况
- **类型**: 桌面视觉小说引擎（Desktop App，Python + pygame）
- **技术栈**: Python 3.8+ / pygame / CustomTkinter（启动器）/ Pyodide（网页版真引擎）
- **定位**: 用 Markdown 风格语法写 galgame，剧本与引擎分离，可发布为加密包 / 网页 / Ren'Py
- **现有功能数**: 约 22 项核心能力（见下）
- **已有输出形态**: 桌面 exe / 加密 `.stmdec` / 网页 HTML / Ren'Py `.rpy` / GUI 启动器

## 现有功能清单（含成熟度）

| 功能 | 成熟度 | 说明 |
|---|---|---|
| Markdown 风格剧本语法 | polished | 缩进即分支，文中内联标记 |
| 多立绘 + 站位（left/center/right/farleft/farright） | functional | `pos`/`tag` 控制，无缩放旋转 |
| 三通道音频（BGM/音效/语音） | polished | 独立音量，互不打断 |
| 内联渲染（粗/斜/删除/等宽/颜色/字号/注音） | polished | `custom/markdown.py` 可扩展 |
| 存档读档 / 历史回顾 / 自动 / 快进 / 打字机 / 设置 | polished | `Ctrl` 快进、`A` 自动、`F5/F9` 存读档 |
| 语法检查器 `--check`（五级错误） | functional | 逐行报错 |
| 游戏内错误界面（不崩命令行） | functional | 但仍偏开发者向措辞 |
| 加密发布 `.stmdec` | functional | PBKDF2+HMAC，防小白级 |
| 缺素材占位块 | polished | 不崩 |
| GUI 启动器（新建/检查/试跑/打包/发布） | polished | CustomTkinter |
| 网页版（Pyodide 跑真引擎） | functional | localStorage 存档 1 自动 + 6 手动 |
| 转 Ren'Py `stm2renpy.py` | functional | 单向尽力转换 |
| 反向 Ren'Py→STMG `renpy2stm.py` | functional | 图层/对话/变量/输入 |
| 受控文件操作 `stm.os`（DDLC 式） | polished | 读/建/移回收站/改+备份，静默 |
| `R.api` 联网 | functional | key 走 options.stm |
| `STM.python` 开发扩展（白名单） | functional | 发布版禁用 |
| `custom/gui.py` 界面自定义 | polished | 对话框/名字框/立绘/标题可改 |
| 项目内嵌引擎（自包含可魔改） | polished | 增量拷贝不覆盖 |
| 无头自测 `selftest.py`（48 项） | polished | 改完先跑它 |
| 询问玩家输入 `Question:` | polished | 结果存 `STM.ANSWER` |
| 选择支 `Choose:` + `If 选项` | polished | 可嵌套 |
| 变量与表达式求值 `SET` | polished | 支持中文变量名 |

## 功能建议（按投入分组，组内按优先级分降序）

优先级分 = Impact×3 + (4−Effort)×2，Effort：低=1/中=2/高=3，Impact：低=1/中=2/高=3，范围 5–15。

### 🟢 快速见效（低投入，优先做）
| 功能 | 分类 | 投入 | 回报 | 分 |
|---|---|---|---|---|
| 跳过已读（已看过的台词一键快进到底） | 用户体验 | 低 | 高 | 15 |
| 错误提示措辞友好化 | 开发者体验 | 低 | 中 | 12 |
| 立绘逐句控制（scale / 透明度 / 微调 y） | 功能扩展 | 低 | 中 | 12 |
| 打字机逐字音效 | 用户体验 | 低 | 中 | 12 |

### 🟡 值得投入（中投入，规划做）
| 功能 | 分类 | 投入 | 回报 | 分 |
|---|---|---|---|---|
| 转场特效（淡入淡出 / 溶解 / 闪白） | 功能扩展 | 中 | 高 | 13 |
| 随机内建 `RAND()`（骰子/概率分支） | 功能扩展 | 中 | 中 | 10 |
| 立绘表情差分（叠图层换表情） | 功能扩展 | 中 | 中 | 10 |
| 立绘旋转 / 漂浮 / 呼吸微动 | 功能扩展 | 中 | 中 | 10 |
| 成就 / 收集系统 | 内容/运营 | 中 | 中 | 10 |
| 存档改为状态快照（修随机分支读档错位） | 性能优化 | 中 | 中 | 10 |

### 🔵 长远目标（高投入，列入路线图）
| 功能 | 分类 | 投入 | 回报 | 分 |
|---|---|---|---|---|
| CG 回廊 / 回想模式 | 功能扩展 | 高 | 高 | 11 |
| 可视化剧本编辑器（拖拽分镜） | 开发者体验 | 高 | 高 | 11 |
| 手机 / 移动端打包 | 集成/生态 | 高 | 高 | 11 |
| 多语言切换 i18n | 功能扩展 | 高 | 中 | 8 |
| 网页版支持 R.api / stm.os | 集成/生态 | 高 | 中 | 8 |

## 每个建议的详细说明

### 跳过已读 ★（最推荐）
- **描述**: 玩家二周目时，已读过的台词可一键快进到下一个未读/选项处，而不是逐句按。galgame 标配，体验提升极大。
- **实现方案**: session 已记录 `say_count` 与 `runtime.history`；在 gui 的快进逻辑里加「是否全部已读」判定——`Ctrl` 快进时若当前 block 的 say_count 低于历史最大值则加速，遇到 `choose`/`question`/未读则停。`custom/gui.py` 可加 `skip_read_only` 开关。
- **风险/前置**: 需区分「本局已读」vs「历史最高已读」，可用 `runtime.history` 长度做判定，几乎零依赖。

### 错误提示措辞友好化
- **描述**: 把「角色『X』没有定义就直接说话了」这类措辞面向作者优化，并给非开发者作者更直白的修复指引。README 已列为已知限制。
- **实现方案**: 在 `errors.py` 的 `Issue` 文案库里加一层「作者向」模板（保留行号+原因）；`gui.py` 错误界面分「作者模式/玩家模式」两套文案（玩家模式只给一句温和提示，已有基础）。
- **风险/前置**: 纯文案改动，动 `errors.py` + `gui.py` 错误界面渲染。

### 立绘逐句控制（scale / 透明度 / 微调 y）
- **描述**: 现在 `S.character` 只接受 `pos`/`tag`，逐句改大小/透明度要改 `custom/gui.py`（全局）。剧本里直接写更自然。
- **实现方案**: 解析器已支持 kwargs（`_sprite_event` 已拆 `pos`/`tag`），扩展接收 `scale`/`alpha`/`y`，透传给 `session.scene["sprites"][tag]` 并让 `render.py` 的 `fit_into` / `draw` 用上。无需新语法关键字，向后兼容。
- **风险/前置**: 仅改 runtime 的 `_sprite_event` + render 的立绘绘制；存档 replay 不受影响（这些是展示态）。

### 打字机逐字音效
- **描述**: 每打出一个字播一个轻音效，是 galgame 经典质感。
- **实现方案**: gui 打字机循环里，每 reveal 一个字符触发一次 `audio.play_se_once("type.wav")`（复用已有三通道音频）；`custom/gui.py` 加 `type_se` / `type_se_volume` 配置项，默认关。
- **风险/前置**: 复用 `audio.py`，加一个可选音效路径即可；注意连续播放的音效排队不要爆音。

### 转场特效（淡入淡出 / 溶解 / 闪白）★
- **描述**: README 已知限制第一条就是「没有转场特效，只做图层切换」。这是视觉品质的最大短板。
- **实现方案**: 新增 `S.transition("fade", 0.6)` 或沿用 Ren'Py 的 `with fade` 风；runtime 产出 `transition` 事件，gui 在切 bg/立绘时叠加一个 alpha 蒙版过渡帧。`custom/gui.py` 加默认转场与时长。
- **风险/前置**: 需动 render（蒙版）+ gui（过渡调度），是中等工作量；无头 `selftest` 不受影响（事件照常 yield）。

### 随机内建 `RAND()`
- **描述**: 现在想随机只能 `STM.python("random")`（开发模式，发布版禁用）。剧本里直接写 `SET x = RAND(1,6)` 或 `If RAND(0,100) < 30` 会极大丰富玩法。
- **实现方案**: `runtime.eval_expr` 的 `SAFE_BUILTINS` 加 `randint`/`RAND`，让 `SET x = RAND(1,6)` 合法；`eval_cond` 同样支持。
- **风险/前置**: ⚠️ 会和「记录选择+重放」的存档机制冲突（读档后随机数结果对不上）。需把随机结果也写进 `runtime.answers` 流（视为一种「回答」）才能正确 replay，或另起「状态快照」存档方案（见下）。

### 立绘表情差分 / 旋转漂浮微动
- **描述**: 同一角色换表情现在要换整张立绘文件；轻微浮动/呼吸/旋转能让画面活起来。
- **实现方案**: 表情差分=立绘图层支持 `expr=` 拼后缀路径或叠半透明差分图；微动=render 对立绘 surface 做 `pygame.transform.rotate` / `sin` 位移（每帧小幅度）。两者都可经 `custom/gui.py` 开关。
- **风险/前置**: 微动需 gui 主循环每帧更新，注意性能；表情差分依赖素材命名约定。

### 成就 / 收集系统
- **描述**: 玩家触发特定变量/选项组合时解锁成就，增强重复游玩动力。
- **实现方案**: 新增 `ACHIEVE("攻略菲线")` 语法 → runtime 维护 `unlocked` 集合并落盘（复用 `save.py` 的持久槽）；gui 做解锁弹窗。`custom/` 里可放成就定义。
- **风险/前置**: 需新增存档字段 + 一个解锁提示 UI；纯增量，不影响现有流程。

### 存档改为状态快照（修随机分支读档错位）
- **描述**: 当前存档只记「选项+say_count 并重放」，遇到 `R.api`/随机数分支读档会对不上（README 已知限制）。
- **实现方案**: `session.snapshot` 改为序列化 `runtime.vars` + `chosen` + 已 yield 到哪一步的进度指针，读档直接 restore 状态而非重放。向后兼容旧档（检测字段缺失则回退重放）。
- **风险/前置**: 中等工作量，需同时改 runtime 的 `reset(replay=)` 与 session 的 snapshot/restore；是随机/`R.api` 类功能的前置依赖。

### CG 回廊 / 回想模式
- **描述**: 解锁过的 CG/背景可在一处回看，galgame 标配收藏功能。
- **实现方案**: runtime 在 `bg`/`picture` 事件经过时把路径记进 `seen_cg`，落盘（复用 save 持久槽）；gui 做一个画廊界面（网格缩略图，点开看大图）。
- **风险/前置**: 高工作量（画廊 UI + 缩略图生成），但复用现有图片加载；与成就系统可共用持久槽。

### 可视化剧本编辑器（拖拽分镜）
- **描述**: 在启动器里做可视化编辑：左侧素材树、中间分镜时间线、右侧属性面板，导出 `.stm`。降低非程序员上手门槛。
- **实现方案**: 在 launcher.py（CustomTkinter）里加一个编辑器页签；内部把 AST ↔ UI 双向绑定；复用 `parser.parse_text` 反序列化回源码。
- **风险/前置**: 高工作量，是面向生态扩张的大功能。

### 手机 / 移动端打包
- **描述**: 头部 `Pack=` 字段现已预留给「以后上手机」。把引擎封到 Android/iOS（如 via BeeWare/Chaquopy 或 Kivy 套壳）可触达更大玩家群。
- **实现方案**: 复用现有 Runtime/Session（不依赖 pygame 也可无头跑逻辑），UI 层替换为移动端渲染；`Pack` 字段启用为包名。
- **风险/前置**: 高工作量，且需重写 pygame 渲染层为移动端友好方案；可作为独立分支。

### 多语言切换 i18n
- **描述**: 一份剧本多语言，玩家在设置里切。
- **实现方案**: 头部 `Lang=` 已预留；新增 `script.<lang>.stm` 或台词加 `[lang=en]` 标记，runtime 按当前语言过滤渲染。
- **风险/前置**: 高工作量，涉及解析器与全部台词结构改造；回报中等（小语种 galgame 需求有限）。

### 网页版支持 R.api / stm.os
- **描述**: 现在网页版静默跳过联网与文件操作（README 已知限制），导致含这两样的剧本网页版表现不全。
- **实现方案**: `web_bridge.py` 用 `fetch` 异步实现 `R.api`（结果回传 Python）；`stm.os` 改为操作浏览器虚拟文件系统（IndexedDB），明确告知玩家是沙箱。
- **风险/前置**: 高工作量，且 `stm.os` 的「真磁盘」语义在浏览器里天然不成立，需重新定义边界。

## 总结建议
先做 🟢 三件零风险小改：**跳过已读**、**立绘逐句 scale/alpha**、**打字机音效**——它们都只动现有层、向后兼容、半天能上线。
接着投入 **转场特效**（视觉品质最大短板）和 **随机内建 + 状态快照存档**（互相依赖，解锁概率玩法）。
CG 回廊、可视化编辑器、手机打包属路线图级，建议等核心体验打磨稳后再开。

> 想直接动手的话，告诉我上面任意一个（或多个），我可以从改动最小的开始实现。
