<
Size=1280x720
title="STMG 开发教程"
Ver="1.0.0"
Pack="com.example.stmg.tutorial"
Lang="cn"
Font="Microsoft YaHei"
>

<
Start:
"欢迎来到 STMG 开发教程。这是一个能直接跑的示范工程，边玩边学怎么写 .stm 剧本。"
"点鼠标 / 空格继续，按 A 可自动播放。下面用「边看边讲」的方式把语法、缩进、常用命令过一遍。"
"先选一章开始：在目录里输入序号（1~6）回车即可；学完会自动回到目录，输入 0 退出。"

S.jump("目录")

<-- ============ 第一章 ============ -->
ch1:
"【第一章 · 文件骨架与缩进】"
"一个 .stm 文件由三个 < > 块组成，靠顺序区分：头、正文、结局。"
"    <"
"    Size=1280x720"
"    title="STMG 开发教程""
"    >"
"    <"
"    Start:"
"    ...这里写正文..."
"    >"
"    <"
"    "感谢游玩""
"    >"
"缩进是 4 个空格（或 1 个 Tab），缩进 = 进入上一层分支，跟 Python 一个道理。"
"头块写 Size / title / Ver / Font；正文以 Start: 开头；结局块可以省略。"
"本章结束，回到目录选下一章吧～"
S.jump("目录")

<-- ============ 第二章 ============ -->
ch2:
"【第二章 · 台词与角色】"
"台词写在一对引号里；前面加角色名（不带引号）就是「让谁说」。"
"    "这是旁白，不显示名字框。""
"    主角"这是主角说的话。""
"    主角: "冒号写法也行。""
"让角色说话前要先注册，名字或立绘路径都行："
"    S.character("主角")"
"    S.character("png/主角.png", pos="left", tag="主角")"
"注册过名字，后面才能用「主角"..."」这种写法，否则引擎会报「角色未定义」。"
"本章结束，回到目录选下一章吧～"
S.jump("目录")

<-- ============ 第三章 ============ -->
ch3:
"【第三章 · 注释与变量】"
"注释用 <-- ... --> 包起来，不会显示，专门给开发者看（你正在看的这段就是注释）。"
"    <-- 这是一行注释 -->"
"变量第一次出现必须用 SET 赋值，之后直接写名字引用。"
"    SET 好感度 = 0"
"    SET 好感度 = 好感度 + 10"
"    SET 名字 = "小明""
"变量名可以是中文；SET 之后就能在台词里拼："
"    "你好，" + 名字 + "。""
"本章结束，回到目录选下一章吧～"
S.jump("目录")

<-- ============ 第四章 ============ -->
ch4:
"【第四章 · 选择支与条件】"
"选择支用 Choose:，选项写在下面并缩进；每个选项配一个 If "选项名": 分支。"
"    Choose:"
"        "去图书馆":"回宿舍""
"    If "去图书馆":"
"        S.cg("bg/图书馆.png")"
"        "图书馆很安静。""
"    If "回宿舍":"
"        S.cg("bg/宿舍.png")"
"    EndIf"
"注意：If "选项名" 判断的是「这个选项有没有被【选过】，状态会一直保留（适合一次性分支）。"
"想做「每次都重新判断」的条件，用变量：If 好感度 >= 10:"
"本教程顶部的目录菜单，就是用 Question + If STM.ANSWER == "1" 这种变量判断做成的循环。"
"本章结束，回到目录选下一章吧～"
S.jump("目录")

<-- ============ 第五章 ============ -->
ch5:
"【第五章 · 询问输入】"
"用 STM.Q 设提示文字，Question: 后面等玩家打字，结果在 STM.ANSWER。"
"    STM.Q = "请问你叫什么名字？""
"    Question:"
"    "原来叫 " + STM.ANSWER + " 啊。""
"提示文字也能直接缩进写在 Question: 下面。"
"（本教程的目录导航就靠 Question + STM.ANSWER 实现，输入 0 还能退出哦。）"
"本章结束，回到目录选下一章吧～"
S.jump("目录")

<-- ============ 第六章 ============ -->
ch6:
"【第六章 · 跳转·标签·结束】"
"在正文顶层写「标签名:」就是跳转点，用 S.jump("标签名") 跳过去。"
"    目录:"
"    S.jump("目录")"
"    S.end()"
"S.end() 直接结束并进结局画面。本教程的「返回目录」就是每章结尾的 S.jump("目录")。"
"台词里还能内联 Markdown 标记，下面这行会被引擎直接渲染成效果："
"    **加粗**  *斜体*  ~~删除线~~  `等宽`"
"    [color=#ff88bb]红色文字[/color]   {ruby:汉字|かんじ}"
"全部章节都看完啦！回到目录输入 0 就能结束，去改 script.stm 写你自己的游戏吧！"
S.jump("目录")

<-- ============ 结局 ============ -->
exit:
S.end()

<-- ============ 目录（循环菜单） ============ -->
目录:
"═══════ STMG 教程目录 ═══════"
"  1 文件骨架与缩进      2 台词与角色"
"  3 注释与变量          4 选择支与条件"
"  5 询问输入            6 跳转·标签·结束"
"  0 退出教程"
"输入序号后回车（这就是 Question 的用法示范）："
STM.Q = "请输入章节序号（1~6 看章节，0 退出）："
Question:
If STM.ANSWER == "1":
    S.jump("ch1")
If STM.ANSWER == "2":
    S.jump("ch2")
If STM.ANSWER == "3":
    S.jump("ch3")
If STM.ANSWER == "4":
    S.jump("ch4")
If STM.ANSWER == "5":
    S.jump("ch5")
If STM.ANSWER == "6":
    S.jump("ch6")
If STM.ANSWER == "0":
    S.jump("exit")
EndIf
"（没看懂输入，回到目录重来～）"
S.jump("目录")
>
