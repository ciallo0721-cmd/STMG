# -*- coding: utf-8 -*-
"""这个项目的界面配置 —— 想改哪就改哪，不改也能跑。

引擎启动时会读这个文件里的 CONFIG，和默认值合并：
**只写你想改的项，没写的用引擎默认值**，整个文件删掉也能跑。

数值说明：
    比例类     写 0~1 的小数（相对屏幕宽 / 高）
    颜色       写 (R, G, B) 或 (R, G, B, A)，取值 0~255
    图片/音乐  写相对项目目录的路径（写成绝对路径也行），文件不存在会自动回退

下面每项后面都跟着注释掉的默认值，想要哪项就把它前面的 # 去掉再改。
改完直接启动游戏就能看到效果。
"""

CONFIG = {
    # ================ 对话框 ================
    # "box_x": 0.05,               # 左边距
    # "box_y": 0.03,               # 距屏幕底部
    # "box_w": 0.90,               # 宽
    # "box_h": 0.30,               # 高（想让框矮一点就调小）
    # "box_radius": 10,            # 圆角
    # "box_color": (252, 252, 255),
    # "box_alpha": 232,            # 0~255，越小越透
    # "box_border": 2,             # 边框粗细，0 = 不画
    # "box_border_color": (150, 152, 175),
    # "text_pad": 26,              # 台词距框内边
    # "text_size": 23,             # 正文字号

    # ================ 名字框 ================
    # "name_size": 22,
    # "name_bg": (72, 96, 140),
    # "name_color": (255, 255, 255),
    # "name_h": 0.075,

    # ================ 立绘 ================
    # "sprite_x": {"left": 0.24, "center": 0.5, "right": 0.76,
    #              "farleft": 0.13, "farright": 0.87},
    # "sprite_w": 0.42,            # 立绘最大宽（占屏宽）
    # "sprite_h": 0.76,            # 立绘最大高（占屏高）
    # "sprite_bottom": 0.78,       # 脚底落在屏幕的哪个高度

    # ================ S.picture 贴图 ================
    # "pic_w": 0.6,
    # "pic_h": 0.6,
    # "pic_center_y": 0.5,         # 贴图底边的高度

    # ================ 标题界面 ================
    # "title_bg": "bg/title.png",      # 标题背景图（放一张图进去就有背景了）
    # "title_pic": "png/logo.png",     # 标题图，不写就找 gui/button/title.png
    # "title_bgm": "music/title.mp3",  # 标题曲，进游戏时会自动停
    # "title_bgm_volume": 0.7,
    # "title_y": 0.18,                 # 标题纵向位置
    # "title_size": 0.075,             # 标题字号（占屏高）
    # "title_top": (30, 32, 56),       # 没有背景图时的渐变上边色
    # "title_bottom": (70, 62, 106),   # 渐变下边色

    # ================ 选择支 ================
    # "choice_w": 0.52,
    # "choice_h": 54,
    # "choice_gap": 16,
    # "choice_size": 22,

    # ================ 询问输入框 ================
    # "ask_x": 0.20, "ask_y": 0.52, "ask_w": 0.60, "ask_h": 0.20,
    # "ask_size": 20,

    # ================ 右上角提示 ================
    # "toast_size": 16,
}
